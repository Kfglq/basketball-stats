# ==============================================================================
# Basketball Stats System - Environment Configuration Template
# 複製此檔案並重新命名為 .env.development (本地) 或在部署平台設定環境變數
# ==============================================================================

# --- Server Configuration ---
APP_PROTOCOL=http
APP_HOST=localhost
APP_PORT=5000
APP_ROUTE_PREFIX=/api

# --- Security ---
# 重要：本地開發可用預設值，但部署時請務必更換為長隨機字串
JWT_SECRET=your_secret_key_here

# --- Database Configuration ---
# 方案 A：
# 如果設定了 DB_URL，程式將優先使用此連線字串，並忽略下方的個別設定
# 格式：postgres://user:password@host:port/dbname?sslmode=require
DB_URL=

# 方案 B：
# 當 DB_URL 為空時，程式會自動拼接以下變數
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=side
DB_SSL_MODE=disable # 雲端連線建議改為 require
